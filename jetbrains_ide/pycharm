#!/bin/bash
#Generated by JetBrains Toolbox 1.25.12627 at 2022-09-02T17:09:29.261779

declare -a ideargs=()
declare -- wait=""

for o in "$@"; do
  if [[ "$o" = "--wait" || "$o" = "-w" ]]; then
    wait="-W"
    o="--wait"
  fi
  if [[ "$o" =~ " " ]]; then
    ideargs+=("\"$o\"")
  else
    ideargs+=("$o")
  fi
done

open -na "/Users/uzaaft/Library/Application Support/JetBrains/Toolbox/apps/PyCharm-P/ch-0/222.4167.4/PyCharm 2022.2 EAP.app/Contents/MacOS/pycharm" $wait --args "${ideargs[@]}"