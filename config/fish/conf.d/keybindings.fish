# Custom key bindings

# Ctrl+G for git repository navigation
bind \cg git_go
bind -M insert \cg git_go

# Ctrl+Space to accept autosuggestion (moved here from plugins.fish)
bind -M insert \c\  accept-autosuggestion
bind \c\  accept-autosuggestion